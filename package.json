{
	"type": "module",
	"dependencies": {
		"cbmp": "^1.1.1"
	},
	"scripts": {
		"clean": "rm -rf bin bitmaps hyprcursor-build dist",
		"render": "cbmp render.json",
		"generate": "cbmp render.json"
	}
}
